# Rename Dictionary

Use this file to freeze the old-to-new identifier map before implementation starts. Do not begin code renames until all shipped identities that matter to a given phase have an approved replacement.

## Placeholder Tokens

Use neutral placeholders until the fork's branding is finalized.

- `<new-publisher>`: VS Code publisher or package owner.
- `<new-brand>`: product family name replacing trademark-bearing branding.
- `<new-cli-command>`: CLI/tool command replacing `dotnet-interactive`.
- `<new-dotnet-prefix>`: .NET namespace and package prefix.
- `<new-ts-prefix>`: TypeScript/browser API prefix.
- `<new-browser-bundle>`: shipped JS asset name.
- `<new-notebook-id>`: VS Code notebook type identity.
- `<new-kernel-title>`: Jupyter kernel display title.

## Status Legend

- `proposed`: candidate replacement exists but is not yet approved.
- `approved`: safe to implement in the assigned phase.
- `deferred`: intentionally kept for compatibility or later cleanup.
- `legacy-alias`: old name retained temporarily after the new name ships.

## Dictionary Fields

| Old identifier                                          | Proposed new identifier                                     | Identifier class | Owning path or package                                                 | First change phase | Compatibility alias | Last old-name support phase | Validation proof                                             | Status   | Notes                                                                               |
| ------------------------------------------------------- | ----------------------------------------------------------- | ---------------- | ---------------------------------------------------------------------- | ------------------ | ------------------- | --------------------------- | ------------------------------------------------------------ | -------- | ----------------------------------------------------------------------------------- |
| `Microsoft.dotnet-interactive`                          | `polyglossy.interactive.tool`                               | artifact         | `src/dotnet-interactive/dotnet-interactive.csproj`                     | 1                  | yes                 | 2                           | renamed tool builds and `--help` works                       | approved | PackageId naming convention to verify during Phase 1 packaging.                     |
| `dotnet-interactive`                                    | `polyglossy-interactive`                                    | artifact         | `src/dotnet-interactive/dotnet-interactive.csproj`                     | 1                  | yes                 | 2                           | local tool invocation plus VS Code transport startup         | approved | Highest-risk rename because extension and parser defaults depend on it.             |
| `Microsoft.DotNet.Interactive`                          | `Polyglossy.Interactive`                                    | public API       | `src/Microsoft.DotNet.Interactive/Microsoft.DotNet.Interactive.csproj` | 5                  | yes                 | 6                           | .NET build plus focused namespace-sensitive tests            | approved | Validate generated contracts and extension loaders during Phase 5.                  |
| `@microsoft/polyglot-notebooks`                         | `@polyglossy/polyglossy-notebooks`                          | artifact         | `src/polyglot-notebooks/package.json`                                  | 3                  | optional            | 3                           | `npm run compile` and `npm test` in `src/polyglot-notebooks` | approved | Npm package scope and name are part of the Phase 3 artifact rename.                 |
| `microsoft.dotnet.interactive.js`                       | `polyglossy.interactive.js`                                 | artifact         | `src/polyglot-notebooks-browser/package.json`                          | 3                  | optional            | 3                           | browser package build and tests                              | approved | Keep served runtime path compatible if bundle file name moves early.                |
| `dist/dotnet-interactive.js`                            | `dist/polyglossy-interactive.js`                            | artifact         | `src/polyglot-notebooks-browser/package.json`                          | 3                  | yes                 | 7                           | browser contract tests plus .NET asset discovery             | approved | Keep old served asset path alive until Phase 7 compatibility is retired.            |
| `createDotnetInteractiveClient`                         | `createPolyglossyInteractiveClient`                         | public API       | `src/polyglot-notebooks-browser/src/library-init.ts`                   | 7                  | yes                 | 8                           | browser package tests and manual notebook JS output scenario | approved | Keep old global delegating to new function for one migration window.                |
| `getDotnetInteractiveScope`                             | `getPolyglossyInteractiveScope`                             | public API       | `src/polyglot-notebooks-browser/src/library-init.ts`                   | 7                  | yes                 | 8                           | browser package tests and checked-in notebook snippets       | approved | Existing notebook-authored JavaScript may call the old global.                      |
| `dotnet-interactive-vscode`                             | `polyglossy-interactive-vscode`                             | artifact         | `src/polyglot-notebooks-vscode/package.json`                           | 3                  | no                  | 3                           | VSIX build and install succeeds                              | approved | Extension marketplace identity change is one-way; treat carefully.                  |
| `ms-dotnettools`                                        | `polyglossy-tools`                                          | artifact         | `src/polyglot-notebooks-vscode/package.json`                           | 3                  | no                  | 3                           | VSIX install under the new publisher                         | approved | Marketplace publisher ownership must be confirmed before publishing.                |
| `Polyglot Notebooks`                                    | `Polyglossy Notebooks`                                      | artifact         | `src/polyglot-notebooks-vscode/package.json`                           | 3                  | no                  | 3                           | VSIX install shows the new display name                      | approved | Covers extension display name and localized package title strings.                  |
| `Microsoft Corporation`                                 | `Polyglossy Tools`                                          | artifact         | `src/polyglot-notebooks-vscode/package.json`                           | 3                  | no                  | 3                           | VSIX manifest metadata uses the new publisher/author         | approved | Covers extension author and publisher-owned metadata.                               |
| `ms-dotnettools.dotnet-interactive-vscode`              | `polyglossy.polyglossy-interactive-vscode`                  | persisted ID     | `src/polyglot-notebooks-vscode/package.json`                           | 9                  | yes                 | 9                           | existing kernelspec metadata resolves to installed extension | approved | Full extension ID appears in Jupyter tests and installed-user metadata.             |
| `dotnet-interactive.acquire`                            | `polyglossy-interactive.acquire`                            | persisted ID     | `src/polyglot-notebooks-vscode/package.json`                           | 9                  | yes                 | 9                           | command palette and activation tests                         | approved | Old command should remain registered during the migration window.                   |
| `dotnet-interactive.kernelTransportArgs`                | `polyglossy-interactive.kernelTransportArgs`                | persisted ID     | `src/polyglot-notebooks-vscode/package.json`                           | 9                  | yes                 | 9                           | settings migration and notebook startup                      | approved | Read old setting as fallback before removing it.                                    |
| `dotnet-interactive.notebookParserArgs`                 | `polyglossy-interactive.notebookParserArgs`                 | persisted ID     | `src/polyglot-notebooks-vscode/package.json`                           | 9                  | yes                 | 9                           | parser open/save scenario                                    | approved | Same migration rule as transport args.                                              |
| `dotnet-interactive.kernelTransportWorkingDirectory`    | `polyglossy-interactive.kernelTransportWorkingDirectory`    | persisted ID     | `src/polyglot-notebooks-vscode/package.json`                           | 9                  | yes                 | 9                           | settings migration and notebook startup                      | approved | Read old setting as fallback before removing it.                                    |
| `dotnet-interactive.interactiveToolSource`              | `polyglossy-interactive.interactiveToolSource`              | persisted ID     | `src/polyglot-notebooks-vscode/package.json`                           | 9                  | yes                 | 9                           | acquisition settings migration and tool startup              | approved | Read old setting as fallback before removing it.                                    |
| `dotnet-interactive.minimumDotNetSdkVersion`            | `polyglossy-interactive.minimumDotNetSdkVersion`            | persisted ID     | `src/polyglot-notebooks-vscode/package.json`                           | 9                  | yes                 | 9                           | SDK requirement validation uses the new setting key          | approved | Read old setting as fallback before removing it.                                    |
| `dotnet-interactive.requiredInteractiveToolVersion`     | `polyglossy-interactive.requiredInteractiveToolVersion`     | persisted ID     | `src/polyglot-notebooks-vscode/package.json`                           | 9                  | yes                 | 9                           | tool version validation uses the new setting key             | approved | Read old setting as fallback before removing it.                                    |
| `onNotebook:dotnet-interactive`                         | `onNotebook:polyglossy-interactive`                         | persisted ID     | `src/polyglot-notebooks-vscode/package.json`                           | 9                  | yes                 | 9                           | notebook activation fires for old and new notebook IDs       | approved | Covers activation event migration.                                                  |
| `dotnet-interactive`                                    | `polyglossy-interactive`                                    | persisted ID     | `src/polyglot-notebooks-vscode/package.json`                           | 9                  | yes                 | 9                           | existing notebooks open and new notebooks use the new type   | approved | Notebook type or kernel ID surface; distinct from the CLI command row above.        |
| `dotnet-interactive-window`                             | `polyglossy-interactive-window`                             | persisted ID     | `src/polyglot-notebooks-vscode/package.json`                           | 9                  | yes                 | 9                           | interactive window opens under old and new IDs               | approved | Requires dual registration or migration where supported.                            |
| `polyglot-notebook`                                     | `polyglossy-notebook`                                       | persisted ID     | `src/polyglot-notebooks-vscode/package.json`                           | 9                  | yes                 | 9                           | existing notebooks open, new notebooks use new type          | approved | Requires explicit migration approach because notebooks may already embed this type. |
| `polyglot-notebook-window`                              | `polyglossy-notebook-window`                                | persisted ID     | `src/polyglot-notebooks-vscode/package.json`                           | 9                  | yes                 | 9                           | interactive window opens under old and new IDs               | approved | Requires dual registration or migration where supported.                            |
| `.NET Interactive (C#)`                                 | `Polyglossy Interactive (C#)`                               | persisted ID     | `src/polyglot-notebooks-vscode/package.json`                           | 9                  | yes                 | 9                           | Jupyter kernel registration and acquisition                  | approved | Display titles can move sooner than IDs if needed.                                  |
| `.NET Interactive (F#)`                                 | `Polyglossy Interactive (F#)`                               | persisted ID     | `src/polyglot-notebooks-vscode/package.json`                           | 9                  | yes                 | 9                           | Jupyter kernel registration and acquisition                  | approved | Display titles can move sooner than IDs if needed.                                  |
| `.NET Interactive (PowerShell)`                         | `Polyglossy Interactive (PowerShell)`                       | persisted ID     | `src/polyglot-notebooks-vscode/package.json`                           | 9                  | yes                 | 9                           | Jupyter kernel registration and acquisition                  | approved | Display titles can move sooner than IDs if needed.                                  |
| `dotnet tool run dotnet-interactive -- notebook-parser` | `dotnet tool run polyglossy-interactive -- notebook-parser` | persisted ID     | `src/polyglot-notebooks-vscode/package.json`                           | 9                  | yes                 | 9                           | parser open/save scenario invokes the new command            | approved | Parser command wiring should keep the old command path during migration.            |

## Approval Checklist

Before a row changes from `proposed` to `approved`, confirm all of the following.

- The replacement does not use Microsoft-owned trademark-bearing product identity.
- The replacement matches the fork's naming convention across .NET, TypeScript, VS Code, and docs.
- The first-change phase is consistent with [phased-rename-plan.md](./phased-rename-plan.md).
- Compatibility intent is explicit for any client-server or persisted-state seam.
- There is at least one command or scenario that proves the new name works.

## Review Notes

- Do not guess final names in code from this file until legal/product naming is approved.
- Add rows whenever a new shipped identity is discovered in [rename-surface-inventory.md](./rename-surface-inventory.md).
- Keep deferred legacy aliases in this file until they are intentionally removed.
